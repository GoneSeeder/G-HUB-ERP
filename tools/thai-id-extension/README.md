# Thai ID Scanner Extension

Development install:

1. Open Chrome or Edge.
2. Go to `chrome://extensions`.
3. Enable Developer mode.
4. Click `Load unpacked`.
5. Select this folder: `tools/thai-id-extension`.
6. Copy the extension ID and put it into the native host manifest before testing native messaging.

Production requires publishing the extension or controlled enterprise deployment so the extension ID is stable.
