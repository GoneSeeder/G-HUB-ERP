import { HrNoCompanyPage } from '@/components/humansource/hr-auth-flow-pages';

export const metadata = {
  title: 'ไม่มีข้อมูลบริษัท — HumanSource HR',
};

export default function HrNoCompanyRoute() {
  return <HrNoCompanyPage />;
}
