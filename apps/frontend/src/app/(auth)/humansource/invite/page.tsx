import { HrInvitePage } from '@/components/humansource/hr-auth-flow-pages';

export const metadata = {
  title: 'คำเชิญ — HumanSource HR',
};

export default function HrInviteRoute() {
  return <HrInvitePage />;
}
