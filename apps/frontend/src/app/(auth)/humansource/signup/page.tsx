import { HrSignupPage } from '@/components/humansource/hr-auth-flow-pages';

export const metadata = {
  title: 'สมัครใช้งาน — HumanSource HR',
};

export default function HrSignupRoute() {
  return <HrSignupPage />;
}
